package Aula4_lacos;

public class Array {
    public static void main(String[] args) {
        double max Double. NEGATIVE_INFINITY; // pegar o menor valor possivel ( menos infinito )
        for (int i = 0 i < n; i++) 
            if (a[i] > max) max = a[i];
    }
}
// notas.length() --> verifica o tamanho de uma array
//  notas[0].length() 